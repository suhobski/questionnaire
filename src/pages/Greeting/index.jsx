import React, { useContext } from "react";
import { AppContext } from "../../context";

const Greeting = () => {
  return (
    <div>
      <h1>Greeting</h1>
    </div>
  );
};

export default Greeting;
